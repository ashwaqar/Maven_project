---
  - hosts: local
    become: true
    tasks:
     - name: remove docker images
       shell: docker image rm -f ashwaqar/maven_demo
       
     - name: create docker image
       shell: docker run -d --name maven_demo -p 8090:8080 ashwaqar/maven_demo
